rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow read and write access for authenticated users.
      // This is a secure rule for production.
      allow read, write: if request.auth != null;
    }
  }
}
