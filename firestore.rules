
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: These rules are insecure and should not be used in production.
    // They allow anyone to read and write to your entire database.
    // TODO: Secure your database before deploying to a live environment.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
