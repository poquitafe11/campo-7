// This file is no longer used as the Firebase Admin SDK is not allowed in this environment.
// All database operations must use the client-side Firebase SDK.
export function getFirebaseAdmin() {
  return null;
}
